# 小白部署步骤

你不用懂代码，只需要按顺序做三件事：

1. 在 Supabase 创建数据库
2. 在 Render 部署后端
3. 在 Vercel 部署网页

我已经把代码准备好了，你主要负责登录自己的账号、复制粘贴几个地址。

## 第一步：Supabase 数据库

打开：

```text
https://supabase.com
```

操作：

1. 注册/登录 Supabase。
2. 点击 `New project`。
3. 随便填项目名，比如：

```text
account-usage
```

4. 设置一个数据库密码，自己保存好。
5. Region 选择离你近的即可。
6. 创建完成后，左侧点 `SQL Editor`。
7. 新建 Query。
8. 打开本项目里的文件：

```text
supabase/schema.sql
```

9. 把里面全部内容复制进去，点击 `Run`。

完成后告诉我：`Supabase 建表完成`。

## 第二步：拿到数据库连接串

在 Supabase 项目里：

1. 左下角点 `Project Settings`。
2. 点 `Database`。
3. 找到 `Connection string`。
4. 选择 `URI` 格式。
5. 复制形如下面这样的地址：

```text
postgresql://postgres.xxxxxx:你的密码@aws-xxx.pooler.supabase.com:6543/postgres
```

注意：如果里面有 `[YOUR-PASSWORD]`，要替换成你刚才设置的数据库密码。

这个地址是敏感信息，不要发到公开地方。你可以在私聊/本机里填。

## 第三步：准备 GitHub 仓库

如果你已经有 GitHub 账号：

1. 打开：

```text
https://github.com
```

2. 新建一个仓库，例如：

```text
account-usage-cloud
```

3. 把这个目录上传到 GitHub：

```text
D:\WaytoAGI\做网站\account-usage-cloud
```

如果你不会上传，停在这里告诉我：`我不会上传 GitHub`，我再带你一步步点。

## 第四步：Render 部署后端

打开：

```text
https://render.com
```

操作：

1. 注册/登录 Render。
2. 点击 `New`。
3. 选择 `Web Service`。
4. 连接你的 GitHub 仓库。
5. Root Directory 填：

```text
backend
```

6. Build Command 填：

```text
npm install
```

7. Start Command 填：

```text
npm start
```

8. 添加环境变量：

```text
DATABASE_URL=你的 Supabase 数据库连接串
INVITE_CODE=你想设置的邀请码
SESSION_SECRET=随便一串很长的随机字符
FRONTEND_ORIGIN=http://localhost:5173
```

`SESSION_SECRET` 可以先用类似：

```text
change-this-to-a-long-random-secret-2026
```

9. 点击 Deploy。

部署成功后，Render 会给你一个地址，比如：

```text
https://account-usage-cloud-api.onrender.com
```

把这个地址记下来。

## 第五步：Vercel 部署前端

打开：

```text
https://vercel.com
```

操作：

1. 注册/登录 Vercel。
2. 点击 `Add New Project`。
3. 选择你的 GitHub 仓库。
4. Root Directory 选择：

```text
frontend
```

5. Framework 一般会自动识别为 `Vite`。
6. 添加环境变量：

```text
VITE_API_URL=你的 Render 后端地址
VITE_WS_URL=wss://你的 Render 后端域名/ws
```

例如：

```text
VITE_API_URL=https://account-usage-cloud-api.onrender.com
VITE_WS_URL=wss://account-usage-cloud-api.onrender.com/ws
```

7. 点击 Deploy。

部署成功后，Vercel 会给你一个网页地址，比如：

```text
https://account-usage-cloud.vercel.app
```

## 第六步：回到 Render 修改 FRONTEND_ORIGIN

因为刚才还不知道 Vercel 地址，所以现在需要回 Render 改一次。

在 Render 后端项目里：

1. 点 `Environment`。
2. 找到：

```text
FRONTEND_ORIGIN
```

3. 改成你的 Vercel 地址：

```text
https://account-usage-cloud.vercel.app
```

4. 保存。
5. 重新部署一次 Render。

## 最后测试

打开你的 Vercel 网页：

1. 点击注册。
2. 输入账号、显示名称、密码。
3. 邀请码填写你在 Render 里设置的 `INVITE_CODE`。
4. 登录后点击 `开始使用`。
5. 用另一个浏览器或手机打开同一个网页，应该能看到谁正在使用。

## 你可以直接把进度发我

你不用解释技术细节，只要按下面格式告诉我卡在哪里：

```text
我做到第 X 步，页面显示：……
```

或者把报错文字截图/复制给我，我继续帮你处理。
