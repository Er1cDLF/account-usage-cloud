# 学习共享空间：项目与聊天记录总结

> 整理时间：2026-09-17。依据本次聊天中提供的截图、终端输出及当前本地仓库代码。历史状态可能已经变化；涉及线上配置的地方以服务器和平台控制台的实际状态为准。
>
> 本文件不保存数据库密码、邀请码、资料管理口令、会话密钥、AccessKey 或其他凭证。聊天中曾出现过凭证，不能因为它们在聊天里出现过就视为可继续使用。

## 1. 项目是什么

“学习共享空间”是一个供成员协调使用两个学习账号的网页。成员先注册并登录，再分别查看账号 A、账号 B 的使用状态；同一学习账号允许多人同时使用。网页记录谁在使用、使用时段、日历、历史和聊天。页面分为“账号总览 / 日历 / 成员 / 聊天 / 历史记录”，避免把所有信息堆在一页。

项目代码在本机 `D:\WaytoAGI\做网站\共享账号在线网页\account-usage-cloud`，GitHub 仓库为 `https://github.com/Er1cDLF/account-usage-cloud`。代码结构：`frontend/` 是 React + Vite + FullCalendar；`backend/` 是 Node.js + Express + WebSocket；`supabase/` 存数据库建表 SQL。项目根目录的 `README.md` 和 `小白部署步骤.md` 主要描述早期 Render + Vercel 方案，**不是当前 ECS 部署指南**。

## 2. 功能与页面

| 页面或功能 | 当前代码中的行为 |
| --- | --- |
| 注册/登录 | 注册使用邀请码；账号名只允许小写字母、数字、下划线，长度 3–32 位，不能用邮箱作账号名。新注册成员需等待管理员授权，授权后才能登录。密码保存为哈希，不能查看原始密码。 |
| 账号总览 | 两张卡片分别显示账号 A、B 的在线人数和具体成员；本人可开始或结束使用。账号 A 显示 `Lifuduan2000@gmail.com`，账号 B 显示 `hzy1026931135@gmail.com`。中间有带 ChatGPT 图标的“启动GPT”，跳转 `https://chatgpt.com/`。 |
| 日历 | 可看全部、账号 A 或账号 B；按成员颜色显示事件，标题为“号A-人名”或“号B-人名”，不展示具体时间。 |
| 成员 | 展示当前已授权人数，按组 A、组 B、未分组列出姓名和颜色。`eric` 为管理员，可在成员页面授权或取消授权，并修改成员分组。实时人数必须登录网页查看；聊天记录和本地代码不能证明当前数据库中的实际注册人数。 |
| 聊天 | 一个公共聊天区，成员对话和系统动态各占一列。离开网页标签后，新成员消息可更新页面标题；浏览器允许时会弹出系统通知。 |
| 历史记录 | 账号 A、B 各占一列，记录使用者、持续时间、开始和结束时间、结束原因。 |
| 个人资料 | 右上角成员名打开菜单，可查看账号名、用管理口令修改密码、选择默认分组、注销账号和退出登录。原始密码不可查看。 |
| OTP 验证码 | 登录成员可在账号卡片中通过九宫格手势查看账号 A/B 的 6 位 TOTP。密钥只保存在后端环境变量中；OTP 接口同时要求有效登录令牌和正确手势。 |

网页标题、Logo 和文案曾多次调整。当前代码使用 `frontend/public/Logo.png`、`frontend/public/favicon.png` 和 `frontend/public/ChatGPT-Logo.png`。品牌文案为：“人以想象开路，AI以智慧相伴；让知识流动，让成长发生。” 用户曾提供 `D:\WaytoAGI\做网站\Logo.png`，后换为 `Logo2.png`；ChatGPT 图标后来处理成白色透明效果，按钮和部分文字也放大过。

用户曾讨论过让成员编辑 Markdown 文档和在 Markdown 中放图片，但当前五个页面中**没有 Markdown 编辑模块**；这属于讨论过的后续功能，不能记成已完成。

## 3. 成员分组与颜色

当前后端 `backend/src/server.js` 的预设如下。颜色同时用于成员页、在线名单、聊天和日历。

| 组别 | 成员 | 颜色 |
| --- | --- | --- |
| 组 A | Eric | `#0A2463` |
| 组 A | 叶子 | `#059244` |
| 组 A | Caesar | `#AC5326` |
| 组 A | lens | `#F6E4D0` |
| 组 A | Litchi | `#E85D75` |
| 组 B | wzh | `#FBB9BA` |
| 组 B | 不要洋葱cong（也匹配 cong） | `#D4E5F4` |
| 组 B | Fentanyl | `#926AAD` |
| 组 B | Maningbo | `#A72061` |
| 组 B | baekhyun | `#8FE3CF` |
| 未分组 | 其他成员 | `#FFD93D` |

用户最初提供过一组候选色值，之后又指定了上述具体成员颜色。Fentanyl 曾临时改过情侣配色，最后恢复为 `#926AAD`；baekhyun 保留与其他成员不同的 `#8FE3CF`。实际线上分组和颜色还可能受数据库里成员自行修改的分组影响，需以运行中网页为准。

## 4. 部署经过

1. **早期方案：Vercel + Render + Supabase。** Vercel 托管前端，Render 跑后端，Supabase 存 PostgreSQL。Vercel 地址曾为 `https://account-usage-cloud.vercel.app/`。聊天截图显示 Vercel Hobby 工作区因用完 5 GB 免费带宽被暂停，导致网站打不开；截图中的“Deployment Ready”并不代表工作区仍能提供访问。
2. **备选平台研究。** 讨论过 Koyeb，但用户看到其验证信用卡/付费页，没有卡，因此没有继续。也讨论过 Codex Sites；它可以托管网站，但这次项目没有确认已迁至 Sites。
3. **阿里云 ECS。** 用户根据 `https://blog.ilz6.com/post/6127.html` 购买了香港 ECS。教程里的第二台“保活”服务器与流量控制脚本不是运行本项目的必需条件，也无法保证抢占式实例永不被回收。聊天里曾贴出阿里云 AccessKey，随后用户表示已经删除；不应在本项目中重用。
4. **服务器配置。** 截图显示实例规格 `ecs.t6-c4m1.large`，2 vCPU、512 MiB 内存，Alpine Linux v3.23，按量的抢占式实例。公网弹性 IP 为 `47.76.133.100`。安装了 Node.js、npm、Git、Nginx 等，并创建 swap。安全组新增了 80/443 端口；截图里还存在向全网开放的 SSH 22 和不适用于 Linux 的 RDP 3389，后者建议核实后删除。
5. **代码和数据库。** 服务器代码位于 `/opt/account-usage-cloud`，曾执行 `git checkout 0741fba`，处于 detached HEAD。数据库继续使用新加坡区域的 Supabase 项目 `account-usage`，项目 ID `inaagkwlngtmuqecyfnm`。Supabase 曾暂停，用户点过 Resume。数据库连接方式选的是 Transaction pooler，连接串和密码不记录在本文件。
6. **后端和网页启动。** 后端在本机 10000 端口启动；用户执行 `curl http://127.0.0.1:10000/health` 得到 `{"ok":true}`。Nginx 通过 80 端口提供 `frontend/dist`，并把 `/api/`、`/ws` 转发到后端；`nginx -t` 成功，Nginx 已加入开机启动。用户随后确认能打开网页。PM2 曾用于管理后端，但聊天中没有证据证明它已配置为开机自动恢复。

目前聊天中**最后一个已确认可访问的网址**是 `http://47.76.133.100`。这只是当时的确认，不代表整理文件时进行了线上探测。它仍是 HTTP，登录密码在公网传输时缺乏 HTTPS 保护，暂不适合作为长期公开入口。

## 5. 网址与 HTTPS 的下一步

用户想把 IP 地址换成好记的网址，并同意不用原来的 `vercel.app` 地址。`account-usage-cloud.vercel.app` 归 Vercel 管理，不能像自有域名那样直接改 DNS 指向 ECS。通过 Vercel 代理或跳转虽然有技术可能，但仍依赖已暂停的 Vercel 工作区。

最近讨论的下一步是注册一个 DuckDNS 免费子域名，在 DuckDNS 控制台把它指向 `47.76.133.100`，**只把新域名（不要把 DuckDNS token）发回聊天**，然后在 ECS 上配置 HTTPS 并测试登录、聊天和 WebSocket。用户尚未报告已注册 DuckDNS 域名，因此这里没有可确认的新网址。长期使用也可以购买自有域名，DNS 指向同一 ECS，再配置 HTTPS。香港服务器是否继续可用还取决于抢占式实例及公网 IP 的实际状态。

## 6. 环境变量与账号恢复

ECS 后端使用 `backend/.env`，需要的变量名包括 `DATABASE_URL`、`INVITE_CODE`、`SESSION_SECRET`、`PROFILE_PASSPHRASE`、`FRONTEND_ORIGIN`、`PORT`。前端当前代码使用同源 `/api`，由 Nginx 代理。若修改域名，需要复核 `FRONTEND_ORIGIN` 是否与最终网页地址一致。先前聊天里出现过 Markdown 链接格式误粘到 `.env` 的情况，环境变量值必须是纯文本网址。

Supabase 数据库密码创建后不能直接在控制台查看，忘记时需要重置；数据库连接串中的 `[YOUR-PASSWORD]` 是占位符。聊天中曾暴露数据库密码，应确认已经重置，并同步修改 ECS 上的 `DATABASE_URL`。不要把 `.env` 或完整连接串贴进聊天、提交到 GitHub。

网页成员忘记密码时，不能找回原密码，只能重置。聊天中提供过一个临时 Node 脚本更新 `app_users.password_hash` 的思路；此前的一行命令因 Shell 解释 `$1`、`$2` 及 ESM 语法问题失败。重置时应在服务器上操作，先核对实际 `username`，完成后删除临时脚本；不要把新密码贴进聊天。

另有一个需要修正的安全问题：当前前端个人资料菜单把静态管理口令直接写在页面文字中，后端也有默认口令。即使 `.env` 中另设口令，页面暴露提示仍不合适；正式对外使用前应改为不显示口令，并采用更合适的密码修改/找回机制。

## 7. 费用与运维提醒

用户问过 ECS 的云盘、512 MiB 内存、流量和扣费方式。聊天截图里系统盘曾显示 40 GiB ESSD，实例报价约 `¥0.041284/小时`；按该截图数值连续运行 30 天约 `¥29.72`，**只是历史截图的粗估，不是当前账单或总价**。ECS 计算和云盘通常按时间计费，公网流量、OSS、快照等可能另计。资源总览截图还显示 1 个 OSS Bucket、1 个弹性公网 IP、1 个共享带宽实例和 2 个自动快照策略，需到阿里云账单核对是否产生费用。之前关于 CDT 免费流量的说法也应以购买时的实际活动、地域和账单为准。

费用入口：`https://billing-cost.console.aliyun.com/`；可在阿里云控制台搜索“费用与成本 / 账单详情 / CDT”。账单可能延迟显示。抢占式 ECS 有被回收或中断的风险，所谓“保活脚本”不能消除这个风险；数据库在 Supabase，网站服务器中断不会直接删除 Supabase 数据，但网页会暂时不可用。

## 8. 已知问题与待办

- 在 DuckDNS 或自有域名上配置 HTTPS，再让其他成员使用新网址登录；完成后验证注册、登录、开始/结束使用、聊天通知和 `/ws` 实时更新。
- 核对 ECS 上 `FRONTEND_ORIGIN`、Nginx 配置以及 PM2 是否在重启后自动恢复；不要只凭一次 `/health` 成功判断整个站点正常。
- 核对 Supabase 数据库密码是否已在泄露后重置；检查历史阿里云 AccessKey 是否确已删除，并避免再次粘贴任何凭证。
- 删除不需要的公网 RDP 3389 规则；稳定后限制 SSH 22 的访问来源。
- 检查 OSS Bucket、自动快照、EIP、共享带宽的实际计费和用途；设费用提醒。
- 更新旧 `README.md`/`小白部署步骤.md`，或另写 ECS 部署手册，避免以后照着过时的 Render/Vercel 步骤操作。
- 如需多人协作编辑 Markdown，再单独设计文档页、图片上传、权限、版本记录和存储；当前未实现。

## 9. 常用位置与非敏感检查

| 用途 | 位置或命令 |
| --- | --- |
| 本地仓库 | `D:\WaytoAGI\做网站\共享账号在线网页\account-usage-cloud` |
| ECS 仓库 | `/opt/account-usage-cloud` |
| ECS 后端 | `/opt/account-usage-cloud/backend` |
| ECS 前端 | `/opt/account-usage-cloud/frontend` |
| 健康检查 | `curl http://127.0.0.1:10000/health`，预期 `{"ok":true}` |
| 后端进程 | `pm2 list`、`pm2 logs account-usage-backend --lines 50` |
| Nginx 检查 | `nginx -t`、`rc-service nginx status` |
| 项目 GitHub | `https://github.com/Er1cDLF/account-usage-cloud` |

**资料边界：**本文件是聊天和本地代码的可继续使用的工作记录，不包含实时数据库查询、实时账单、ECS 当前在线状态或新域名的验证结果。
